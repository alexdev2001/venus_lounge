{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ],
  "buildCommand": "npm run prebuild && npm run build",
  "outputDirectory": "build",
  "devCommand": "react-scripts start",
  "cleanUrls": true
}